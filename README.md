# bert-imdb-sentiment-analysis
A full NLP pipeline for classifying tweets dataset to perform sentiment classification

# Dataset Instructions

The original dataset (e.g., `3000.xlsx`) is **not included** in this repository.

To run `train.py`, please place your dataset file inside this `data/` directory:
